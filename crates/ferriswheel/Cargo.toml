[package]
name = "ferriswheel"
version = "0.5.0"
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "RGB LED ring effects and animations"
documentation = "https://docs.rs/ferriswheel"
readme = "README.md"
keywords = ["led", "ring", "ws2812", "neopixel", "rainbow"]
categories = ["embedded", "no-std"]

[features]
smart-leds-compat = ["dep:smart-leds-trait"]

[dependencies]
rgb.workspace = true
smart-leds-trait = { workspace = true, default-features = false, optional = true }
