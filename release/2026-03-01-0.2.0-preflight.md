# Release Pre-flight — 0.2.0 — 2026-03-01

**Target version:** `0.2.0`
**Release type:** minor (new public API added)
**Branch:** `main` @ `e71237b`

## Summary

| Check | Result | Notes |
|---|---|---|
| Version consistency | PASS | All crates need bumping to 0.2.0 — handled in plan step 1 |
| Branch & git state | PASS | On `main`, working tree clean |
| Build | PASS | `just ci` clean |
| Tests | PASS | 177 tests pass (147 ferriswheel, 19 led-effects, 11 ws2812-pure) |
| Changelog | PASS | `[Unreleased]` section present with content; needs version header |
| Security (deny) | PASS | `cargo deny` — advisories, licenses, sources all ok |
| Security (audit) | PASS | `cargo audit` — 939 advisories checked, 0 vulnerabilities found (160 deps) |

## Findings

### Version consistency

Current state (pre-bump):

| Crate | Current | Target |
|---|---|---|
| `ws2812-pure` | 0.1.0 | 0.2.0 |
| `ferriswheel` | 0.1.0 | 0.2.0 |
| `led-effects` | 0.1.1 | 0.2.0 |
| `rustyfarian-esp-idf-ws2812` | 0.1.0 | 0.2.0 |
| `rustyfarian-esp-hal-ws2812` | 0.1.0 | 0.2.0 |

All five `Cargo.toml` files need updating. No version field in the workspace root `Cargo.toml` — per-crate only.

### Branch & git state

- Branch: `main` ✓
- Working tree: clean ✓
- No unpushed commits detected
- No existing tags — this is the first release

### Build and tests

`just ci` ran clean:

- `cargo fmt -- --check` — PASS
- `cargo deny check` — PASS (warnings for duplicate transitive deps from ESP-IDF toolchain — these are expected and benign, all flagged as `warn` not `deny`)
- `cargo check` — PASS
- `cargo clippy -- -D warnings` — PASS
- `cargo test` — PASS (177 tests, 0 failed)

### Changelog

`CHANGELOG.md` has an `[Unreleased]` section with:

**Added:**
- `NoLed` stub in `led-effects`
- `RainbowEffect::with_hue_offset(u8)` builder
- `RainbowEffect::set_hue_offset(&mut self, u8)` live update
- `PulseEffect::set_color(&mut self, RGB8)` live update
- `SpinnerEffect::set_color(&mut self, RGB8)` live update
- `ChaseEffect::set_color(&mut self, RGB8)` live update
- `FlashEffect::set_color(&mut self, RGB8)` live update

**Fixed:**
- `PulseEffect::new()` doc comment correction
- `SpinnerEffect` tail brightness arithmetic cleanup

Needs `## [Unreleased]` replaced with `## [0.2.0] - 2026-03-01` and a fresh empty `[Unreleased]` added above.

### Security

`cargo deny check` — PASS. Advisories, licenses, bans, and sources all clean.
Duplicate transitive dependencies (`bitflags`, `embedded-hal`, `syn`, etc.) are all from the ESP-IDF toolchain chain — expected in this ecosystem, not actionable.

`cargo audit` — PASS. 939 advisories checked, 0 vulnerabilities found across 160 crate dependencies.

## Go / No-Go

**PASS — ready to release.**

No blocking issues. Proceed to the release plan.
